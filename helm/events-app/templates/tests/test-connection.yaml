apiVersion: v1
kind: Pod
metadata:
  name: {{ include "events-app.fullname" . }} - test-connection
  labels:
    {{- include "events-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "events-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
